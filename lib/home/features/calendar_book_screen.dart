import 'package:flutter/material.dart';
import '../main_layout.dart';

class CalendarBookScreen extends StatelessWidget {
  const CalendarBookScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'Cart Screen',
      style: TextStyle(
        fontSize: 24,
        fontWeight: FontWeight.bold,
        color: Colors.deepPurple,
      ),
    );
  }
}
